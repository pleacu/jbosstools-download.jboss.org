<?xml version="1.0" encoding="UTF-8"?>
<project default="run">
  <!-- run locally in Eclipse (to resolve p2 tasks) using these commandline options:
    java -cp /home/nboldt/eclipse/plugins/org.eclipse.equinox.launcher_*.jar \
      org.eclipse.equinox.launcher.Main -consoleLog -nosplash -data /tmp \
      -application org.eclipse.ant.core.antRunner -f build.xml
  -->
  <property name="SHORTNAME" value="kepler"/>
  <property name="version" value="20130510-0900-M7"/>

  <property name="destination" value="/home/nboldt/tru/download.jboss.org/jbosstools/updates/requirements/${SHORTNAME}/" />
  <property name="workDir" value="${destination}/${version}"/>

  <target name="run">
    <ant antfile="../generic/build.xml" target="run">
      <property name="SHORTNAME" value="${SHORTNAME}"/>
      <property name="LONGNAME" value="Eclipse Kepler"/>
      <property name="version" value="${version}"/>
      <!-- <property name="URL" value="http://download.eclipse.org/releases/staging/" /> -->
      <property name="URL" value="http://download.eclipse.org/releases/kepler/201303220900/" />
   </ant>
  </target>

  <!-- because mirroring can take a while, you can use these targets to simply pack200 an existing mirror or regenerate metadata -->
  <target name="pack"><ant antfile="${destination}/../regen.metadata.xml" target="pack.artifacts"></ant>
  <target name="regen"><ant antfile="${destination}/../regen.metadata.xml" target="regen.metadata"></ant>
  </target>
</project>
